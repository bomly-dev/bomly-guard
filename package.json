{
  "name": "bomly-review-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action wrapper for bomly diff",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "package": "ncc build lib/main.js -o dist",
    "all": "npm run build && npm run package"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.2"
  }
}
